var React = require('react');

var PageContainer = React.createClass({
  render: function() {
    return(
      <div id="reqAdmin">
        List of Resources
      </div>
    )
  }
});

export const ResourceIndex = PageContainer;
